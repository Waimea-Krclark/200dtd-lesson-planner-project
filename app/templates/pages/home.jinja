{#=====================================================
  Home page
  - Week display
  - Lists lessons for each day
=====================================================#}

{% extends 'pages/base.jinja' %}


{% block title %}

    Lesson Planner

{% endblock %}


{% block content %}

    <article class="week_viewer">
        {% for day in days %}

            <article class="day_article" onclick="location.href='/day/{{ day.code }}'">
                <h2>
                    <span class="day_short">{{ day.code | capitalize  }}</span>
                    <span class="day_long">{{ day.name  }}</span>
                </h2>
                {% for lesson in lessons %}

                    {% if lesson.day_code == day.code %}

                        <p class="lesson_display">
                            <span class="lesson_time">{{ lesson.time | nicetime }}</span> 
                            <span class="lesson_name">{{ lesson.name }}</span>
                        </p>

                    {% endif %}

                {% else %}
 
                    <p>No Lessons</p>  

                {% endfor %}
            </article>

        {% else %}

            <p>None!</p>

        {% endfor %}

    </article>

{% endblock %}

