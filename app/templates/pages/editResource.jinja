{#=====================================================
  Edit Resource Page
  - Form for updating the data of an existing resource
=====================================================#}

{% extends 'pages/base.jinja' %}


{% block title %}

    Flask Demo - Update Resource

{% endblock %}


{% block content %}


    <article class="formArticle">
        <h3>Edit resource: {{ resource.name }}</h3>

        <form action="/updateResource/{{resource_id}}" method="POST">
            <fieldset role="group">
                <input class="formInputs"
                    type="text"
                    name="name"
                    placeholder="Resource Name"
                    value="{{ resource.name }}"
                    required
                />
            
                <select class="formInputs" name="lesson" aria-label="Select Lesson" required >
                    <option selected disabled value="">
                      <p>{{ current_lesson.name }} (Current Lesson)</p>
                    </option>

                    {% for lesson in lessons %}

                        {% if lesson.name == current_lesson.name %}

                            <option selected>{{ lesson.name }} - {{ lesson.day_code | upper}}</option>
                            
                        {% else %}

                            <option>{{ lesson.name }} - {{ lesson.day_code | upper }}</option>

                        {%endif%}

                    {% endfor %}
                </select>
            </fieldset>
            
            <input class="formInputs"
                type="text"
                name="link"
                placeholder="Paste Resource Link"
                value="{{ resource.link }}"
                required
            />

            <input class="formInputs"
                type="text"
                name="notes"
                placeholder="Short Note For Resource"
                value="{{ resource.notes }}"
            >

            <input class="primaryButton" type="submit" value="Update Resource"/>
        </form>
    </article>

{% endblock %}

